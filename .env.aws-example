# AWS Configuration for Photo Uploads
# Copy these values to your .env file

# AWS Region
AWS_REGION=us-east-1

# AWS Credentials (get from ~/.aws/credentials)
AWS_ACCESS_KEY_ID=your_access_key_here
AWS_SECRET_ACCESS_KEY=your_secret_key_here

# S3 Bucket Name
AWS_S3_BUCKET=oriva-user-photos

# Instructions:
# 1. Copy these variables to your .env file
# 2. Replace placeholder values with your actual AWS credentials
# 3. Ensure your AWS IAM user has permissions for:
#    - s3:PutObject, s3:GetObject, s3:HeadObject, s3:DeleteObject
#    - rekognition:DetectModerationLabels
# 4. Configure S3 bucket with CORS (see api/routes/PHOTOS_API.md)
