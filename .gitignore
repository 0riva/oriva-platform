node_modules
.DS_Store
.env
dist
.vercel

# Security - Internal implementation files
# api/ - Commented out to allow API deployment
archive/
internal-docs/

# Agent and workflow files (internal use only)
.claude/
.specify/
CLAUDE.md
AGENTS.md
CLAUDE_DEPLOYMENT_RULES.md
AGENT.md
*_AGENT.md
*_WORKFLOW.md

# Internal review and audit documents
DOCUMENTATION_ARCHITECTURE_REVIEW.md
DOCUMENTATION_FIXES_APPLIED.md
DOCUMENTATION_IMPROVEMENTS.md
SECURITY_AUDIT_REPORT.md
SECURITY_INCIDENT*.md
PRE_DEPLOYMENT_ISSUES.md

# Environment files
.env.local
.env.production
.env.staging

# Logs
*.log
logs/

# Database files
*.db
*.sqlite
*.sqlite3

# Build artifacts
build/
out/
.env*.local

# Security - Debug and temporary files
debug-*.js
temp-*.js
*-debug.js
test-credentials.js
api/debug-*.js

# Coverage reports
coverage/

# Additional Security for Public Repository
*.key
*.pem
*.p12
*.cert
*.crt
*secrets*
*credentials*
*.private
.aws/
.ssh/

# IDE and Editor files
.idea/
.vscode/settings.json
*.swp
*.swo
*~

# OS files
Thumbs.db
.Trashes
.fseventsd

# Backup and temporary files  
*.backup
*.bak
*.tmp
*.temp
*~

# Large files that might cause issues
*.iso
*.dmg
*.pkg
*.exe

# Cache directories
.cache/
.parcel-cache/
# Compiled JavaScript (except main entry point)
api/**/*.js
!api/test-wrapper.js
api/**/*.js.map
api/node_modules/

# --- Secrets: ignore by SHAPE, not by filename (ultra-network#3020) ---
# The previous rules enumerated filenames, so every new variant walked straight
# past them: .env.production, .env.fresh, .env.vercel and ten .bak forms were all
# committed while .env / .env.local / .env*.local were correctly ignored.
# Ignore everything .env-shaped, then allow back only what is meant to ship.
.env*
!.env*example
!.env*sample
!.env.template

# Agent / editor tooling carries credentials too — an OpenAI key reached o-core
# history via .cursor/mcp.json, which no .env rule would ever have caught.
.cursor/
.aider*
