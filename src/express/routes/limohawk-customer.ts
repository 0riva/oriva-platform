/**
 * LimoHawk Customer-facing routes
 * Stub — implement when customer portal is wired up
 */

import { Router } from 'express';

const router = Router();

// Placeholder — add customer-facing loyalty endpoints here

export default router;
